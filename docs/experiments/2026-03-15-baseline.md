# Baseline Run: 2026-03-15

## Goal

Train the first real world-model baseline after the smoke test passed.

## Data

- environment: `CartPole-v1`
- collected episodes: `100`
- max steps per episode: `200`
- dataset path: `data/cartpole`

## Training configuration

- epochs: `5`
- batch size: `8`
- sequence length: `16`
- latent dim: `32`
- hidden dim: `128`
- KL scale: `0.1`
- artifact path: `artifacts/baseline_2026_03_15/world_model.pt`

## Result

The training run completed successfully and saved a checkpoint.

Selected logged metrics:

- step `25`: loss `0.2334`
- step `100`: loss `0.0184`
- step `300`: loss `0.0542`
- step `525`: loss `0.0559`

## What this means

- the pipeline is working end to end on a non-smoke dataset
- the model is learning to reconstruct and predict simple environment signals
- this is a baseline for future comparisons, not a final model

## Why this run matters

The first priority was to prove that:

1. data collection works
2. training works on saved trajectories
3. artifacts can be produced reproducibly

Now we can improve the project without guessing whether the foundation is broken.
